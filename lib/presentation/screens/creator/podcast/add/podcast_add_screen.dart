import 'package:flutter/material.dart';

class PodcastAddScreen extends StatelessWidget {
  const PodcastAddScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
