import 'package:flutter/material.dart';

class PodcastEditScreen extends StatelessWidget {
  const PodcastEditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
