import 'package:flutter/material.dart';

class CreatorPlayScreen extends StatelessWidget {
  const CreatorPlayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
